version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm install
  build:
    commands:
      - echo Build phase - nothing to compile for this simple app

artifacts:
  files:
    - "**/*"
  exclude-paths:
    - .git/**/*
    - node_modules/**/*